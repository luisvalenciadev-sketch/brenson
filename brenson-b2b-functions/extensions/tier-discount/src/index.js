// Punto de entrada que busca la CLI de Shopify para Functions en JS (src/index.js).
// La lógica vive en run.js para poder probarla con node --test sin el runtime de Functions.
export { run } from './run.js';
