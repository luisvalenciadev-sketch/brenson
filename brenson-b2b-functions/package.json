{
  "name": "brenson-b2b-functions",
  "version": "0.1.0",
  "private": true,
  "description": "App custom Shopify con la Function de descuento por tier B2B (precios por volumen sin Shopify Plus).",
  "scripts": {
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "test": "cd extensions/tier-discount && npm test"
  },
  "devDependencies": {
    "@shopify/cli": "^3.80.0"
  },
  "workspaces": ["extensions/*"]
}
